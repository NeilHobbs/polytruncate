track_resistance_no_insecticide_response = function(current.resistance.intensity,
                                                 fitness.selection){


  update.resistance.intensity = current.resistance.intensity + fitness.selection

  return(update.update.resistance.intensity)

}
